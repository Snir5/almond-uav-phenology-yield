# Almond tree segmentation, inference only.
#   pip install -r requirements.txt
#
# Tested on Python 3.11.

# Pinned. The checkpoint stores UNet++ decoder keys in the 0.3.x layout
# (decoder.blocks.x_0_0.*); later releases renamed them and the weights
# will not load.
segmentation-models-pytorch>=0.3.3,<0.4

# Pinned. Albumentations 2.0 changed transform arguments and behaviour,
# so 1.x is needed to reproduce the preprocessing used in training.
albumentations>=1.3,<2.0

torch>=2.0
torchvision>=0.15
opencv-python>=4.7
numpy>=1.24,<2.0
scipy>=1.10
scikit-image>=0.20

# Georeferencing. Drop these four if you do not need GeoTIFF output;
# predict.py detects their absence and writes overlays and masks only.
rasterio>=1.3
pyproj>=3.5
piexif>=1.1
Pillow>=9.5
