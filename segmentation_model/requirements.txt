# Almond tree segmentation, inference only.
#   pip install -r requirements.txt

torch>=2.0
torchvision>=0.15
segmentation-models-pytorch>=0.3.3
albumentations>=1.3
opencv-python>=4.7
numpy>=1.24
scipy>=1.10
scikit-image>=0.20

# Georeferencing. Drop these three if you do not need GeoTIFF output;
# predict.py detects their absence and writes overlays and masks only.
rasterio>=1.3
pyproj>=3.5
piexif>=1.1
Pillow>=9.5
